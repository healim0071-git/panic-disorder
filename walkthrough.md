# 커뮤니티 4대 영역 및 전 페이지 하단 공통 영역 전면 시간순 역순 정렬 (Milestone 9.50)

## 1. 개요
사용자 요청에 따라 **커뮤니티 4대 영역(FAQ·치료후기·유튜브·칼럼)** 및 **모든 페이지 하단에 공통으로 반복 노출되는 FAQ·치료후기·유튜브·칼럼** 전체 영역에서 글 배열 순서를 **가장 최신글이 맨 위(1순위)에 오고 가장 오래된 글이 맨 아래(마지막)에 오도록 엄격한 시간순 역순(내림차순) 정렬**을 구축하고 검증을 완료했습니다.

---

## 2. 기존 문제점 및 원인
1. **커뮤니티 페이지 (`content/community/_index.md`)**:
   - `getBoardData(key, fallback)`에서 로컬 스토리지, 볼트(Vault), 커스텀 글, 시드 데이터를 병합(`merged`)한 후 **시간순 정렬(`.sort`) 과정이 누락**되어 있어, 글이 저장소에 추가된 순서나 시드 배열 순서대로 노출되는 문제 발생.
   - 4개 탭 렌더링 함수(`renderFaqList`, `renderReviewsList`, `renderYoutubeList`, `renderColumnsList`)에서도 정렬되지 않은 원본 배열을 그대로 렌더링함.
2. **전 페이지 하단 공통 영역 (`layouts/_partials/components/common_bottom_sections.html`)**:
   - `getItemTimeScore()`가 13자리 밀리초 타임스탬프, 유튜브 영상 ID(`yt-1` ~ `yt-15`), 칼럼 인덱스(`colIndex`), 시드 번호 간 시간 점수를 정확하게 계량하지 못하여 특정 글의 정렬 순서가 꼬이는 현상 존재.
   - 하단 공통 `defaultYoutubeList`에 3개 영상만 정의되어 있어 전체 15개 영상 목록의 시간순 연동이 미비했음.
   - 하단 정적 HTML 카드(칼럼 등)가 최신 글이 아닌 과거 글(1번~3번)로 하드코딩되어 있어 자바스크립트 로딩 전 최신 글과 불일치.

---

## 3. 해결 및 작업 내역

### 1) 커뮤니티 페이지 (`content/community/_index.md`) 정밀 시간 점수 계산 및 역순 정렬 엔진 탑재
- `getItemTimeScore(item, index, totalLength)` 및 `sortCommunityItemsByTime(items)` 함수 구현:
  - `dateScore`(날짜 밀리초) + `subDayOffset`(당일 밀리초/ID 타임스탬프/인덱스 가중치)를 결합하여 작성 시점(밀리초 단위) 기준으로 엄격한 내림차순 정렬.
  - 새 글을 작성하거나 수정하면 `Date.now()` 밀리초가 반영되어 즉각 최상단(#1)으로 배치.
- `getBoardData()`의 모든 반환점(YouTube 및 FAQ·치료후기·칼럼)에 `merged = sortCommunityItemsByTime(merged);` 적용.
- 4대 탭 렌더링 함수(`renderFaqList`, `renderReviewsList`, `renderYoutubeList`, `renderColumnsList`)에 `sortCommunityItemsByTime()` 2중 정렬 적용.

### 2) 전 페이지 하단 공통 영역 (`common_bottom_sections.html`) 정밀 시간순 정렬 및 최신화
- `getItemTimeScore()`를 정밀화하여 최신 등록 글(ID에 타임스탬프 포함)이 100% 최우선 순위로 계산되도록 개선.
- `defaultYoutubeList`를 3개 -> 전체 15개 영상 전체로 확장.
- 하단 정적 HTML 카드(치료칼럼, 치료후기, FAQ, 유튜브)를 모두 최신 상위 글로 전면 교체:
  - **치료칼럼**: #23 이갈이(2026.09.09), #22 골반통(2026.09.09), #21 미각후각 상실(2026.09.08)
  - **치료후기**: 최신 테스트 후기 3건(2026.09.09) + 임상 후기 3건(2026.09.04, 2026.09.01, 2026.08.27)
  - **자주 묻는 질문(FAQ)**: #22 기립성 어지럼, #21 구강건조증, #20 기상 시 두근거림, #19 목 이물감, #1 병원 검사 정상 (2026.09.09)
  - **유튜브**: yt-1(2026.08.17), yt-2(2026.08.16), yt-3(2026.08.13)

### 3) 전역 커뮤니티 허브 데이터 정렬 일치화
- `data/healim_community_hub.json` 및 `static/data/healim_community_hub.json`의 4대 영역(faq, reviews, columns, youtube) 전체 배열을 최신순(내림차순)으로 영구 동기화.

---

## 4. 검증 결과

### 1) 자동화 종합 정렬 검증 (`scratch/verify_complete_sorting.js`)
- **FAQ (22건)**: 1위 #22(2026.09.09) ~ 22위 #18(2026.07.18) 완벽 정렬 확인.
- **치료후기 (9건)**: 1위 최신 후기(2026.09.09) ~ 9위 #6 후기(2026.08.10) 완벽 정렬 확인.
- **치료칼럼 (23건)**: 1위 #23(2026.09.09) ~ 23위 #1(2026.08.06) 완벽 정렬 확인.
- **유튜브 (15건)**: 1위 yt-1(2026.08.17) ~ 15위 yt-15(2026.06.10) 완벽 정렬 확인.
- **새 글 등록 시뮬레이션**: 배열의 어떤 위치(맨 앞, 중간, 맨 끝)에 새 글(`Date.now()`)을 주입해도 4대 영역 모두 즉각 1순위(#1, 맨 위)로 재배치됨을 100% 검증.
- **하단 공통 정적 HTML 카드**: 4대 영역 모두 최신 1위 글과 100% 일치 확인.

### 2) 빌드 검증 (`hugo --minify`)
- Hugo 컴파일 32개 페이지 에러 0건, 1.7초 만에 빌드 완료.
